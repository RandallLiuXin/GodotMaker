# GodotMaker project configuration
# Edit these values to customize behavior
# Deployed to .godotmaker/config.yaml by publish script

# Selected coding-agent runtime for this project.
# Supported values: claude-code, codex, opencode
# publish.py updates this value on each publish.
#
# OpenCode projects must use codex or API-backed selectors for image and VQA
# fields.
agent: claude-code

# Optional godotmaker-cli pipeline model override.
# The GodotMaker framework preserves this block but does not consume it;
# godotmaker-cli owns the supported model IDs and reads this key.
# Leave this commented to use the selected runtime's CLI default.
#
# pipeline:
#   model: <agent-model-id>

# Source-language backend for project-owned gameplay code.
# Supported values: auto, gdscript, csharp
# Existing .NET projects are detected when auto is selected. New scaffolded
# projects still use GDScript until C# project generation is implemented.
language_backend: auto

# Unit test backend.
# Supported values: auto, gdunit, dotnet
# `auto` keeps gdUnit4 for GDScript projects and uses standard .NET test
# commands for existing C# projects when a .sln or test .csproj can be identified.
unit_test_backend: auto

# Optional project-relative .NET solution or test project used when
# unit_test_backend resolves to dotnet.
# dotnet_target: tests/MyGame.Tests/MyGame.Tests.csproj

# Optional project-relative Godot C# project used for build/import checks when
# it is different from dotnet_target.
# godot_csharp_project: MyGame.csproj

# Optional reasoning level override for the selected coding-agent runtime.
# Leave this commented to inherit the agent/user default. Common tested values
# include low, medium, high, and xhigh; provider-specific values may also work
# if the selected CLI accepts them.
#
# reasoning_effort: high

# Visual quality assessment model.
# Supported values:
# - native: active agent runtime image inspection
# - codex: Codex image inspection
# - gemini:<model>: Gemini API through visual_qa.py
# - openai:<model>: OpenAI Responses API through visual_qa.py
vqa_model: native

# Fallback when vqa_model is unavailable.
# Supported values: native, codex, none
vqa_fallback_model: native

# Asset image generation model.
# Supported values:
# - native: active agent runtime image generation
# - codex: Codex image generation
# - gemini:<model>: API-backed source generation through tools/asset_source_generate.py
# - openai:<model>: API-backed source generation through tools/asset_source_generate.py
# - grok:<model>: API-backed source generation through tools/asset_source_generate.py
asset_image_model: native

# Agent model configuration. All roles default to sonnet; raise
# worker_model to opus for games that need heavier implementation reasoning.
worker_model: sonnet
verifier_model: sonnet
reviewer_model: sonnet
analyst_model: sonnet
auditor_model: sonnet
decomposer_model: sonnet
asset_producer_model: sonnet
