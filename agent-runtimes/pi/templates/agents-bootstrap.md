Before using any GodotMaker stage in Pi, read
`.pi/references/runtime-mapping.md`. Pi uses `/skill:gm-<role>` invocation,
project-local `.pi/` resources, the published Godot runtime bridge, and the
extension-backed `godotmaker_delegate` tool for isolated role work.
